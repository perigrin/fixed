#!/usr/bin/env perl
#use strict;

use fixed;

# sub rabbit {
#     fixed ($foo, $bar) = @_;
#     return 1 if $foo <= 0;
#     say $foo;
#     return ( rabbit( $foo - 1 ) + rabbit( $foo - 2 ) );
# }
# 
# is rabbit(3), 5, 'got the right answer';
# 
# done_testing;
